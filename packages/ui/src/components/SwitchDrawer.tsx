import React from "react";
import { View, Text, Pressable, ScrollView, Image } from "react-native";
import type { Instance } from "@wc/core";
import { theme } from "../theme/tokens";
export function SwitchDrawer({ instances, activeSerial, previewSource, onPick, onClose }: { instances: Instance[]; activeSerial: string; previewSource: (serial: string) => { uri: string; headers?: { Authorization: string } }; onPick: (i: Instance) => void; onClose: () => void }) { return <View style={{ position: "absolute", inset: 0 as any }}><Pressable onPress={onClose} style={{ position: "absolute", inset: 0 as any, backgroundColor: "rgba(0,0,0,.58)" }} /><View style={{ position: "absolute", left: 0, right: 0, bottom: 0, backgroundColor: theme.color.surfaceRaised, borderTopWidth: 1, borderColor: theme.color.border, paddingVertical: 16 }}><View style={{ flexDirection: "row", paddingHorizontal: 18, marginBottom: 10 }}><Text style={{ flex: 1, color: theme.color.text, fontFamily: theme.font.monoBold, fontSize: 12 }}>SWITCH INSTANCE</Text><Pressable onPress={onClose}><Text style={{ color: theme.color.textMuted }}>✕</Text></Pressable></View><ScrollView horizontal contentContainerStyle={{ paddingHorizontal: 10 }}>{instances.map((i) => { const active = i.serial === activeSerial; return <Pressable key={i.id} accessibilityLabel={i.title} onPress={() => onPick(i)} style={{ width: 132, marginHorizontal: 6, borderWidth: 1, borderColor: active ? theme.color.accent : theme.color.border, padding: 7 }}><Image source={previewSource(i.serial)} style={{ width: "100%", aspectRatio: 16 / 9, backgroundColor: theme.color.surface }} /><View style={{ flexDirection: "row", gap: 4, marginTop: 7 }}><Text numberOfLines={1} style={{ flex: 1, color: theme.color.text, fontFamily: theme.font.medium, fontSize: 11 }}>{i.title}</Text>{i.active ? <Text style={{ color: theme.color.live, fontFamily: theme.font.monoBold, fontSize: 9 }}>LIVE</Text> : null}</View>{(i.w && i.h) || typeof i.fps === "number" ? <Text style={{ color: theme.color.textDim, fontFamily: theme.font.mono, fontSize: 9, marginTop: 4 }}>{i.w && i.h ? `${i.w}×${i.h}` : ""}{i.w && i.h && typeof i.fps === "number" ? " · " : ""}{typeof i.fps === "number" ? `${i.fps} FPS` : ""}</Text> : null}</Pressable>; })}</ScrollView></View></View>; }
